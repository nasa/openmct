queries:
  - exclude: '*'
  - include:
      tags:
        - 'security'
        - 'correctness'
      severity: 'error'

extraction:
  javascript:
    index:
      exclude:
        # Ignore app.js, should never be used in production
        - app.js
