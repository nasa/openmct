# Open MCT Composition API Example

In this example we will define a custom composition plugin that will dynamically add and remove objects at random to two folders named "NORCAL Approach" and "SFO Approach".